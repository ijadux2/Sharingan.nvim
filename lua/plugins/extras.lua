--------------------------------------------------------------------------------
-- Extra Plugins
-- File: lua/plugins/extras.lua
-- Description: Additional plugins that don't fit in other categories
--
-- CONTENTS:
--   Add any additional plugins here that you want to include
--   but don't belong to the main categories (ui, lsp, tools, filetypes)
--
-- Usage:
--   Copy a plugin spec from other files and paste here
--   Or add new plugins that you want to try
--------------------------------------------------------------------------------

return {
	-- Example:
	-- {
	--     "username/plugin-name",
	--     event = "VeryLazy",
	--     config = function()
	--         -- Plugin configuration
	--     end,
	-- },

	-- Add your extra plugins below:
}
